# 学生抽签系统 - 系统设计文档

## 📋 文档信息

- **项目名称**：学生抽签系统
- **版本**：v0.2.0
- **更新日期**：2025-12-06
- **文档作者**：Kiro AI Assistant

## 🎯 系统概述

学生抽签系统是一个基于 Web 的随机抽签应用，采用前后端分离架构。系统支持从学生数据库中随机抽取学生，并提供性别、班级等多种筛选条件。

### 核心功能

1. **随机抽签** - 公平随机地从学生库中抽取学生
2. **条件筛选** - 支持按性别、班级进行筛选
3. **统计展示** - 实时显示学生总数、性别分布、班级分布
4. **动画效果** - 流畅的抽签动画，提升用户体验
5. **数据管理** - 支持通过数据库工具管理学生数据

## 🏗️ 系统架构

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      用户浏览器                          │
│                 http://localhost:5173                   │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP/JSON
                       │
┌──────────────────────▼──────────────────────────────────┐
│                   Vue 3 前端                             │
│  ┌────────────────────────────────────────────────┐    │
│  │  组件层 (Components)                            │    │
│  │  - StatsCard.vue (统计卡片)                    │    │
│  │  - FilterPanel.vue (筛选面板)                  │    │
│  │  - LotteryBox.vue (抽签盒子)                   │    │
│  └────────────────────────────────────────────────┘    │
│  ┌────────────────────────────────────────────────┐    │
│  │  组合式函数 (Composables)                       │    │
│  │  - useLottery.ts (抽签逻辑)                    │    │
│  └────────────────────────────────────────────────┘    │
│  ┌────────────────────────────────────────────────┐    │
│  │  API 层 (API)                                   │    │
│  │  - lottery.ts (API 调用)                       │    │
│  └────────────────────────────────────────────────┘    │
└──────────────────────┬──────────────────────────────────┘
                       │ RESTful API
                       │ http://localhost:5000/api/lottery
┌──────────────────────▼──────────────────────────────────┐
│              C# ASP.NET Core 后端                        │
│  ┌────────────────────────────────────────────────┐    │
│  │  控制器层 (Controllers)                         │    │
│  │  - LotteryController.cs                        │    │
│  └────────────────────────────────────────────────┘    │
│  ┌────────────────────────────────────────────────┐    │
│  │  服务层 (Services)                              │    │
│  │  - IStudentService.cs (接口)                   │    │
│  │  - StudentService.cs (实现)                    │    │
│  └────────────────────────────────────────────────┘    │
│  ┌────────────────────────────────────────────────┐    │
│  │  数据访问层 (Data Access)                       │    │
│  │  - Dapper ORM                                  │    │
│  └────────────────────────────────────────────────┘    │
└──────────────────────┬──────────────────────────────────┘
                       │ SQL
                       │
┌──────────────────────▼──────────────────────────────────┐
│                  SQLite 数据库                           │
│                  students.db                            │
│  ┌────────────────────────────────────────────────┐    │
│  │  students 表                                    │    │
│  │  - id (主键)                                    │    │
│  │  - student_id (学号)                           │    │
│  │  - name (姓名)                                  │    │
│  │  - gender (性别)                                │    │
│  │  - major (专业)                                 │    │
│  │  - class (班级)                                 │    │
│  └────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

## 🔧 技术栈

### 后端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| .NET | 8.0 | 运行时框架 |
| ASP.NET Core | 8.0 | Web 框架 |
| Dapper | 2.1.66 | 轻量级 ORM |
| Microsoft.Data.Sqlite | 10.0.0 | SQLite 数据库驱动 |
| Swashbuckle.AspNetCore | 10.0.1 | Swagger 文档生成 |

### 前端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.5 | 前端框架 |
| TypeScript | 5.9 | 类型系统 |
| Vite | 7.2 | 构建工具 |
| pnpm | 最新 | 包管理器 |

### 数据库

| 技术 | 版本 | 用途 |
|------|------|------|
| SQLite | 3.x | 嵌入式数据库 |

## 📊 数据库设计

### students 表结构

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY, AUTOINCREMENT | 主键ID |
| serial_number | INTEGER | NULL | 序号 |
| student_id | TEXT | NOT NULL | 学号 |
| name | TEXT | NOT NULL | 姓名 |
| gender | TEXT | NULL | 性别（男/女） |
| major | TEXT | NULL | 专业 |
| class | TEXT | NULL | 班级 |

### 索引设计

```sql
-- 性别索引（用于性别筛选）
CREATE INDEX idx_gender ON students(gender);

-- 班级索引（用于班级筛选）
CREATE INDEX idx_class ON students(class);
```

## 🔌 API 接口设计

### 基础信息

- **Base URL**: `http://localhost:5000/api/lottery`
- **Content-Type**: `application/json`
- **字符编码**: UTF-8

### 接口列表

#### 1. 获取所有学生

**请求**
```http
GET /api/lottery/students
```

**响应**
```json
[
  {
    "id": 1,
    "serialNumber": 1,
    "studentId": "202412001",
    "name": "张三",
    "gender": "男",
    "major": "计算机科学与技术",
    "class": "25计科1班"
  }
]
```

#### 2. 获取单个学生

**请求**
```http
GET /api/lottery/students/{id}
```

**响应**
```json
{
  "id": 1,
  "serialNumber": 1,
  "studentId": "202412001",
  "name": "张三",
  "gender": "男",
  "major": "计算机科学与技术",
  "class": "25计科1班"
}
```

#### 3. 随机抽签

**请求**
```http
GET /api/lottery/draw?gender=男&className=25计科1班
```

**参数**
- `gender` (可选): 性别筛选
- `className` (可选): 班级筛选

**响应**
```json
{
  "id": 1,
  "serialNumber": 1,
  "studentId": "202412001",
  "name": "张三",
  "gender": "男",
  "major": "计算机科学与技术",
  "class": "25计科1班"
}
```

#### 4. 获取统计信息

**请求**
```http
GET /api/lottery/stats
```

**响应**
```json
{
  "total": 266,
  "genderStats": [
    { "gender": "男", "count": 196 },
    { "gender": "女", "count": 70 }
  ],
  "classStats": [
    { "class": "25计科1班", "count": 56 },
    { "class": "25计科2班", "count": 55 }
  ]
}
```

#### 5. 获取班级列表

**请求**
```http
GET /api/lottery/classes
```

**响应**
```json
[
  "25计科1班",
  "25计科2班",
  "25计科3班",
  "25软工班"
]
```

## 🎨 前端设计

### 组件结构

```
App.vue (主应用)
├── StatsCard.vue (统计卡片)
│   ├── 总人数显示
│   ├── 性别分布
│   └── 班级分布
├── FilterPanel.vue (筛选面板)
│   ├── 性别选择器
│   ├── 班级选择器
│   └── 重置按钮
└── LotteryBox.vue (抽签盒子)
    ├── 抽签按钮
    ├── 结果显示
    └── 动画效果
```

### 状态管理

使用 Vue 3 Composition API 进行状态管理：

```typescript
// useLottery.ts
const students = ref<Student[]>([]);          // 所有学生
const selectedStudent = ref<Student | null>(); // 选中的学生
const statistics = ref<Statistics | null>();   // 统计信息
const classList = ref<string[]>([]);          // 班级列表
const isDrawing = ref(false);                 // 是否正在抽签
const filterGender = ref('');                 // 性别筛选
const filterClass = ref('');                  // 班级筛选
```

### 数据流

```
用户操作 → 组件事件 → Composable 函数 → API 调用 → 后端处理 → 返回数据 → 更新状态 → 视图更新
```

## 🔐 安全设计

### CORS 配置

```csharp
builder.Services.AddCors(options =>
{
    options.AddPolicy("AllowVue", policy =>
    {
        policy.WithOrigins("http://localhost:5173", "http://localhost:5174")
              .AllowAnyHeader()
              .AllowAnyMethod();
    });
});
```

### 输入验证

- 后端对所有输入参数进行验证
- 使用参数化查询防止 SQL 注入
- Dapper 自动处理参数转义

## ⚡ 性能优化

### 后端优化

1. **使用 Dapper ORM**
   - 轻量级，性能接近原始 SQL
   - 减少内存占用

2. **数据库索引**
   - 为常用查询字段添加索引
   - 提高查询速度

3. **异步操作**
   - 所有数据库操作使用 async/await
   - 提高并发处理能力

### 前端优化

1. **组件懒加载**
   - 按需加载组件
   - 减少初始加载时间

2. **状态管理**
   - 使用 Composition API
   - 避免不必要的重渲染

3. **动画优化**
   - 使用 CSS 动画
   - GPU 加速

## 📝 开发规范

### 命名规范

**C# 后端**
- 类名：PascalCase（如 `StudentService`）
- 方法名：PascalCase（如 `GetStudentAsync`）
- 私有字段：_camelCase（如 `_logger`）
- 接口：I + PascalCase（如 `IStudentService`）

**Vue 前端**
- 组件文件：PascalCase（如 `StatsCard.vue`）
- 变量/函数：camelCase（如 `selectedStudent`）
- 常量：UPPER_SNAKE_CASE（如 `API_BASE`）
- 类型接口：PascalCase（如 `Student`）

### 注释规范

**C# 后端**
```csharp
/// <summary>
/// 获取所有学生
/// </summary>
/// <returns>学生列表</returns>
public async Task<IEnumerable<Student>> GetAllStudentsAsync()
```

**Vue 前端**
```typescript
/**
 * 执行抽签
 */
async function performDraw() {
  // 实现代码
}
```

## 🧪 测试策略

### 后端测试

1. **单元测试**
   - 测试服务层逻辑
   - 测试数据访问层

2. **集成测试**
   - 测试 API 接口
   - 测试数据库操作

3. **API 测试**
   - 使用 Swagger UI 测试
   - 使用 test-api.bat 脚本测试

### 前端测试

1. **组件测试**
   - 测试组件渲染
   - 测试用户交互

2. **集成测试**
   - 测试 API 调用
   - 测试数据流

## 📦 部署方案

### 开发环境

```bash
# 后端
cd lottery-backend
dotnet run

# 前端
cd lottery-frontend
pnpm install
pnpm run dev
```

### 生产环境

**后端部署**
```bash
cd lottery-backend
dotnet publish -c Release -o ./publish
```

**前端部署**
```bash
cd lottery-frontend
pnpm run build
# 将 dist 目录部署到 Web 服务器
```

## 🔄 版本历史

### v0.2.0 (2025-12-06)
- ✅ 重构后端使用 Dapper ORM
- ✅ 添加 Swagger API 文档
- ✅ 完善前端组件化设计
- ✅ 实现前后端真实串联
- ✅ 添加完整的类型定义
- ✅ 优化用户体验和动画效果

### v0.1.0 (2025-12-06)
- ✅ 初始版本
- ✅ 基础抽签功能
- ✅ 简单的前后端实现

## 📚 参考资料

- [ASP.NET Core 文档](https://docs.microsoft.com/aspnet/core)
- [Dapper 文档](https://github.com/DapperLib/Dapper)
- [Vue 3 文档](https://vuejs.org/)
- [TypeScript 文档](https://www.typescriptlang.org/)
- [Swagger 文档](https://swagger.io/)

---

**文档维护**：本文档随系统更新而更新，请保持文档与代码同步。
