========================================
📦 使用 pnpm 包管理器
========================================

本项目前端使用 pnpm 作为包管理器

========================================
✅ 为什么使用 pnpm
========================================

优势：
✅ 更快的安装速度
✅ 更少的磁盘空间占用
✅ 更严格的依赖管理
✅ 完全兼容 npm

========================================
🔧 安装 pnpm（首次使用）
========================================

方法一：使用 npm 安装（推荐）
npm install -g pnpm

方法二：使用独立安装脚本
PowerShell:
iwr https://get.pnpm.io/install.ps1 -useb | iex

方法三：使用 Chocolatey
choco install pnpm

验证安装：
pnpm --version

========================================
🚀 快速开始
========================================

第1步：安装依赖（首次运行）
   双击：安装依赖-pnpm.bat
   或运行：pnpm install

第2步：启动前端
   双击：start-frontend.bat
   或运行：pnpm run dev

========================================
📝 常用命令对照
========================================

npm 命令              pnpm 命令
----------------------------------------
npm install          pnpm install
npm install vue      pnpm add vue
npm uninstall vue    pnpm remove vue
npm run dev          pnpm run dev
npm run build        pnpm run build
npm update           pnpm update

========================================
🔄 从 npm 迁移到 pnpm
========================================

如果之前使用 npm：

1. 删除旧的依赖（可选）
   删除 node_modules 文件夹
   删除 package-lock.json

2. 安装 pnpm
   npm install -g pnpm

3. 安装依赖
   双击 安装依赖-pnpm.bat
   或运行 pnpm install

4. 启动项目
   双击 start-frontend.bat

========================================
📂 pnpm 文件说明
========================================

生成的文件：
- node_modules/      依赖包（符号链接）
- pnpm-lock.yaml     锁定文件（类似 package-lock.json）
- .pnpm-store/       全局存储（可选）

保留的文件：
- package.json       项目配置（不变）

========================================
🎯 项目脚本
========================================

启动开发服务器：
pnpm run dev
或
pnpm dev

构建生产版本：
pnpm run build
或
pnpm build

预览生产版本：
pnpm run preview
或
pnpm preview

========================================
💡 pnpm 特性
========================================

1. 硬链接和符号链接
   - 节省磁盘空间
   - 多个项目共享依赖

2. 严格的依赖管理
   - 只能访问声明的依赖
   - 避免幽灵依赖

3. 更快的安装速度
   - 并行安装
   - 智能缓存

4. Monorepo 支持
   - 工作空间功能
   - 多包管理

========================================
🔧 配置 pnpm（可选）
========================================

查看配置：
pnpm config list

设置淘宝镜像（加速）：
pnpm config set registry https://registry.npmmirror.com

设置存储位置：
pnpm config set store-dir D:\pnpm-store

========================================
📊 性能对比
========================================

安装速度（相对）：
pnpm:  ⚡⚡⚡⚡⚡ 最快
npm:   ⚡⚡⚡ 中等
yarn:  ⚡⚡⚡⚡ 较快

磁盘占用（相对）：
pnpm:  💾 最少
npm:   💾💾💾 最多
yarn:  💾💾 中等

========================================
⚠️ 注意事项
========================================

1. 确保已安装 pnpm
   运行：pnpm --version

2. 首次使用需要安装依赖
   双击：安装依赖-pnpm.bat

3. 不要混用包管理器
   - 使用 pnpm 就一直用 pnpm
   - 不要同时使用 npm 和 pnpm

4. 锁定文件
   - pnpm-lock.yaml 应该提交到 git
   - 不要删除此文件

========================================
🐛 常见问题
========================================

问题1：pnpm 命令不存在
解决：安装 pnpm
npm install -g pnpm

问题2：安装速度慢
解决：设置国内镜像
pnpm config set registry https://registry.npmmirror.com

问题3：权限错误
解决：以管理员身份运行

问题4：端口被占用
解决：关闭占用 5173 端口的程序

========================================
📚 更多资源
========================================

官方网站：
https://pnpm.io/

中文文档：
https://pnpm.io/zh/

GitHub：
https://github.com/pnpm/pnpm

========================================
🎯 快速命令参考
========================================

安装依赖：
pnpm install

添加依赖：
pnpm add <package>

删除依赖：
pnpm remove <package>

更新依赖：
pnpm update

运行脚本：
pnpm run <script>
或
pnpm <script>

清理缓存：
pnpm store prune

========================================
✅ 已配置完成
========================================

启动脚本已更新为使用 pnpm：
- start-frontend.bat
- 安装依赖-pnpm.bat

现在可以使用 pnpm 了！

========================================
